{
  "name": "giapha-backend",
  "version": "1.0.0",
  "description": "Backend API cho hệ thống quản lý gia phả",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd ../frontend && npm install && npm run build",
    "deploy": "npm run build && npm start",
    "test": "node test-db.js",
    "test:create": "node test-db.js --create-test"
  },
  "keywords": ["giapha", "family-tree", "genealogy"],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.x",
    "npm": ">=6.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sequelize": "^6.35.0",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "express-validator": "^7.0.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
