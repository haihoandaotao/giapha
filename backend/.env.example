# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
# Local: postgresql://user:password@localhost:5432/giapha
# Render will auto-provide DATABASE_URL
DATABASE_URL=postgresql://user:password@localhost:5432/giapha

# JWT Secret
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=30d

# CORS
CORS_ORIGIN=http://localhost:3000
